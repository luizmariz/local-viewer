{
  "name": "local-viewer-example",
  "version": "0.1.0",
  "private": true,
  "description": "Dev-only tooling for the Local Viewer example stack (LocalStack + seed data). The app itself is the Go module — see README.md.",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "example:up": "docker compose -f example/docker-compose.yml up -d",
    "example:down": "docker compose -f example/docker-compose.yml down -v",
    "example:logs": "docker compose -f example/docker-compose.yml logs -f",
    "example:seed": "node example/seed.js",
    "example": "npm run example:up && node example/wait-ready.js && npm run example:seed"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@aws-sdk/client-cloudformation": "^3.700.0",
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/client-sqs": "^3.700.0"
  }
}
